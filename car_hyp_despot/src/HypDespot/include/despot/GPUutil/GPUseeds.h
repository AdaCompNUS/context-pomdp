#ifndef GPUSEEDS_H
#define GPUSEEDS_H

#include <vector>
#include <despot/GPUutil/GPUrandom.h>

namespace despot {

class Dvc_Seeds {
/*private:
	static unsigned root_seed_;
	static Dvc_Random seed_gen_;
	static int num_assigned_seeds_;

public:
	static void root_seed(unsigned value);

	static unsigned Next();

	static std::vector<unsigned> Next(int n);*/
};

} // namespace despot

#endif
